# Coerentis (coerentis.co) — static site at repo root.
# Netlify Forms: contact + whitepaper use data-netlify="true".
# Optional: set DATABASE_URL (Neon), run sql/neon-form-submissions.sql, deploy — submission-created archives to Postgres.
#
[build]
  command = "true"
  publish = "."

[functions]
  directory = "netlify/functions"

